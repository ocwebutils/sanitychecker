{
	"name": "@ocwebutils/sanitychecker",
	"license": "GPL-3.0",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "bun run --cwd 'packages/client' build && bun run --cwd 'packages/server' build",
		"start:client": "bun run --cwd 'packages/client' start",
		"start:server": "bun run --cwd 'packages/client' server"
	},
	"devDependencies": {
		"@commitlint/cli": "17.7.1",
		"@commitlint/config-conventional": "17.7.0",
		"husky": "8.0.3"
	}
}
